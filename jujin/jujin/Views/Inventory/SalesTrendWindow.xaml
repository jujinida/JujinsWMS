<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="jujin.Views.Inventory.SalesTrendWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="판매 추이 분석" Height="600" Width="1000"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#ECEFF4">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 제목 -->
        <TextBlock Grid.Row="0" x:Name="TitleTextBlock" Text="판매 추이 분석" FontSize="24" FontWeight="Bold" 
                   Foreground="#2E3440" Margin="0,0,0,20"/>
        
        <!-- 차트 영역 -->
        <Border Grid.Row="1" Background="White" BorderBrush="#D8DEE9" BorderThickness="1" 
                CornerRadius="5" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Text="최근 1개월 일별 판매량 추이" FontSize="18" FontWeight="Bold" 
                           Foreground="#2E3440" Margin="0,0,0,15"/>
                
                <lvc:CartesianChart Grid.Row="1" x:Name="SalesChart" 
                                    Series="{Binding SeriesCollection}"
                                    LegendLocation="Top">
                    <lvc:CartesianChart.DataTooltip>
                        <lvc:DefaultTooltip SelectionMode="SharedYValues"/>
                    </lvc:CartesianChart.DataTooltip>
                </lvc:CartesianChart>
            </Grid>
        </Border>
        
        <!-- 버튼 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="닫기" Width="80" Height="35" Margin="0,0,10,0"
                    Background="#95A5A6" Foreground="White" BorderThickness="0"
                    Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
