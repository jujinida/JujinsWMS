<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="jujin.Views.Inventory.ReceivingManagementSubPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#ECEFF4">
        <Border BorderBrush="#D8DEE9" BorderThickness="2" CornerRadius="5" Margin="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- 제목과 버튼들 -->
                <Grid Grid.Row="0" Margin="30,20,30,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="입고 관리" FontSize="28" FontWeight="Bold" 
                               Foreground="#2E3440" VerticalAlignment="Center"/>
                    
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button x:Name="ReceivingListBtn" Content="입고 처리" Width="120" Height="40"
                                Background="#5E81AC" Foreground="White" BorderThickness="0"
                                Margin="0,0,10,0" Click="ReceivingListButton_Click"/>
                        <Button x:Name="HistoryBtn" Content="입고내역 조회" Width="140" Height="40"
                                Background="#5E81AC" Foreground="White" BorderThickness="0"
                                Click="HistoryButton_Click"/>
                    </StackPanel>
                </Grid>
                
                <!-- 메인 콘텐츠 영역 -->
                <ContentControl x:Name="MainContentControl" Grid.Row="1" Grid.RowSpan="2"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
